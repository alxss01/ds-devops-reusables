on:
    workflow_call:
        inputs:
            sonar-cloud-enabled:
                description: 'Executar ou não sonar scanner'
                default: true
                required: true
                type: boolean
            owasp-zap-enabled:
                description: 'Executar ou não dependency check'
                default: true
                required: true
                type: boolean
            snyk-enabled:
                description: 'Executar ou não snyk'
                default: true
                required: true
                type: boolean

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Intial
              run: |
                echo "Hello, Alex!"
